export enum Role{
    ADMIN = 'admin',
    USER = 'user',
    SELLER = 'seller',
    GUEST = 'guest',
    MODERATOR
    = 'moderator',
    SUPPORT = 'support',
    MANAGER = 'manager',
    SUPER_ADMIN = 'super_admin',
}